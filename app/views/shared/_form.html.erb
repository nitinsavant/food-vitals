<%= form_for(@submission) do |f| %>

  <%= render 'shared/error_messages', object: f.object %>
  <%= f.text_field :url, class: 'form-control', placeholder: 'Paste recipe link and hit enter.' %>
  <%= f.submit "Submit", class: 'button postfix' %>

<% end %>
